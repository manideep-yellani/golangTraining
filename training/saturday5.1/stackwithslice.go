package main

/*
import "fmt"

func push(a []int, e ...int) []int {
	for _, j := range e {
		a = append(a, j)
	}
	fmt.Println(a)
	return a
}

func pop(a []int) int {
	r := a[len(a)-1]
	a = a[:len(a)-1]
	return r
}

func main() {
	a := []int{}
	a = push(a, 1, 1, 555, 33535, 4)
	pop(a)
	fmt.Println(a)
	//fmt.Println(pop(a))

	//push(a, 2)
}
*/
