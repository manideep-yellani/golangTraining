package main

/*
import "fmt"

type node struct {
	next *node
	val  int
}

func main() {
	h := node{val: 1}
	head := &h

	add(head, 9)
	dis(head)

}

func add(head *node, va int) {
	t := node{}
	t.val = va
	cur := head
	for cur.next != nil {
		cur = cur.next
	}
	cur.next = &t
}

func dis(head *node) {
	cur := head
	for cur != nil {
		fmt.Println(cur.val)
		cur = cur.next
	}
}
*/
